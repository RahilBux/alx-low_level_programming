#include <stdio.h>

/**
 * main - prints numbers 0-9
 * Return: 0(Success)
 */
int main(void)
{
	int num = 0;

		while (num < 10)
		{
			printf("%d", num);
				num++;
		}
	printf("\n");
	return (0);
}
